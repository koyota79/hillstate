<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ShopInforMationDao">
    <select id="selectShopInfoDetail">
        select 
            shop_id ,shop_nm ,open_time ,close_time ,tel_no
           ,position 
        from h_shop_mstr
        where shop_id = %(shop_id)s and use_yn = 'Y'
    </select>

    
    <select id="selectShopMapList">
       select 
              shop_id ,shop_nm ,open_time ,close_time ,tel_no
             ,position ,position_area ,category
         from h_shop_mstr
        where use_yn = 'Y'
		  and position = case when %(position_idx)s != '' then %(position_idx)s else position end
		order by position ,position_area asc ,shop_id
    </select>
    
</mapper>